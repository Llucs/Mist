# Contributing to Mist

## Code style
- No unnecessary comments
- Follow Kotlin conventions
- Use `camelCase` for functions and variables, `PascalCase` for classes

## Before submitting
- Ensure the project builds with `./gradlew assembleDebug`
- Test on a real device if possible

## Pull requests
- Keep them focused on a single change
- Link any related issues
